function countProperties(obj) {
    return Object.keys(obj).length;
  }
  
  // Example usage:
  const sampleObj = { name: "Alice", age: 25, city: "New York" };
  console.log(countProperties(sampleObj)); // Output: 3
  